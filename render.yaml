services:
  - type: web
    name: ai-audit-backend
    env: python
    buildCommand: ""
    startCommand: uvicorn main:app --host=0.0.0.0 --port=10000
    plan: free
    autoDeploy: true
    region: oregon
